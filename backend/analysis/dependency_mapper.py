"""Parse requirements.txt, pyproject.toml, package.json, pom.xml, build.gradle. Return deps by ecosystem with evidence file path."""

from typing import Any


def map_dependencies(repo_id: str) -> dict[str, Any]:
    """Return dependencies grouped by ecosystem with evidence file path."""
    return {}
